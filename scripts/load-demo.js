import {Store} from '../src/db/store.js';import path from 'node:path';const s=new Store(path.resolve(process.env.TABATLAS_STATE_DIR||'state-v2','tabatlas.db'));const tabs=[];for(let i=0;i<240;i++)tabs.push({id:i+1,windowId:1,groupId:i%5,groupTitle:['AI setup','Game inspiration','Read later','Tools','Prompts'][i%5],url:`https://demo.example/resource/${i}?variant=${i%3}`,title:`Demo ${i%4===0?'video tutorial':i%4===1?'article':i%4===2?'artwork reference':'tool guide'} ${i+1}`});console.log(s.capture({runId:'demo-v1',source:'demo',browsers:[{browser:'demo',profile:'synthetic',status:'ok',tabs}]}));s.close();
